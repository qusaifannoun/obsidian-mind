---
date: 2026-06-02
description: "Index of memory topics — key decisions, patterns, gotchas, people context"
tags:
  - brain
  - index
---

# Memories

Persistent context and knowledge retained across sessions. Each topic lives in its own note — follow the links.

- [[Key Decisions]] — architectural and workflow decisions worth recalling
- [[Patterns]] — index of recurring patterns and conventions discovered across work; split into four domain notes on 2026-08-01, **add new entries to the domain note, not the index**
  - [[Patterns - Method & Conventions]] — verification bias, spec-gap routing, ticket hygiene, propose-before-implementing, where a convention must live to be reachable
  - [[Patterns - Architecture & Boundaries]] — backend owns business rules, one rule/one implementation, confirm the far side exists before wiring a control
  - [[Patterns - Frontend & UI]] — forms, tables, uploads, auth gating, cache invalidation and layout across the four frontends
  - [[Patterns - Backend & Domain]] — staff-management wiring, section placement by data ownership, notifications, reject flows, server-generated form PDFs
- [[Gotchas]] — index of things that have bitten before and will bite again; split into six domain notes on 2026-07-28, **add new entries to the domain note, not the index**
  - [[Gotchas - Backend Schema & Data]] — Mongoose document/schema mechanics: enum defaults, index lifecycle, subdocument spreads, sequence allocation
  - [[Gotchas - Backend Services & Environment]] — seeding, notification plumbing, API contract shapes, which DB an environment actually points at
  - [[Gotchas - TOR & Staff Management]] — TOR activation, sit-in eligibility, staff profile and qualification traps
  - [[Gotchas - Forms & Approval]] — Form 32/285, History Form, approval and permission rules, post-approval locking
  - [[Gotchas - Frontend]] — component and framework traps across tat-prereq, tat-portal, tat-ws
  - [[Gotchas - Tooling & Method]] — toolchain, vault tooling, and recurring reasoning failures
- [[People & Context]] — org structure, teams, review history, dynamics
- [[North Star]] — living goals document, read at session start
- [[Skills]] — custom slash commands and workflows

## Recent Context

> [!warning] Gap: **2026-07-09 → 2026-07-11 and 2026-07-13 → 2026-07-29 were never logged here.** Those sessions produced [[Form 32 Rejection History & Round-Scoped Stamps]], [[Pending Review Requests - Reviewer Worklist]], [[TAT-423 Assessment Report Rubric]] and [[Stale Sit-In Index & Orphaned Instructor Enrollments]] — the work notes exist, only this index is behind.

- **2026-08-02** — **The TOR certificate publish gate, built and entirely unverified.** [[TAT-455 Final TOR Certificate - SA Publish Gate]] — instructor visibility of an **approved** Final TOR Certificate became a **separate publication state**, no longer a consequence of QM approval. **Fourth ticket running where Dawahreh shipped the backend with no frontend** (`35736850`); built the FE (`251dde6` + `44c1f92`, [[tat-prereq]]): SA Publish/Unpublish plus the instructor blocked state, driven **purely off `canPublish`/`canUnpublish` from the DTO** so the button cannot disagree with the server — new [[Patterns - Architecture & Boundaries#The control renders from the server's capability flag, never from a client-side role check (2026-08-02)|pattern]]. **Missed AC-05 on the first cut** (it requires publishing from the TOR *details* page, not the certificate sub-page) and fixed it by extracting `TorCertificatePublicationCard` for both views — second occurrence, so extract not fork. Evidence is a live `GET` confirming the contract, plus a **bogus-sibling probe** separating *"not deployed"* (404) from *"guarded"* (403). **Nothing is verified**: no publish round-trip, no instructor 403, no QM/TM view — blocked because **no certificate on staging has ever reached `APPROVED`**, so [[TAT-450 TOR Certificate FE - Read Path Only|TAT-450]]'s write path is still unexercised and gates this. Two traps recorded: the instructor 403 is discriminated by **exact message string** because two different 403s share the endpoint with **no error code** (asked Hamza; not provided), and — **unexplained** — `251dde6` reached `origin/dev` with **no `git push` ever run**, no post-commit hook in either repo, reflog saying *"update by push"*. Until that's explained, **"local only" is an unverified claim in this environment**. **Same day, [[TAT-449 Staff Number Display - Unpadded staffNumber]]** — TAT-449's backend was complete and **two stated requirements were never built**: `grep` for `staffNumber` in [[tat-prereq]] returned **zero hits**, so the allocated-indexed-immutable-backfilled number was displayed nowhere. Built both surfaces (`1e08eca5` + `ab8481f`), and **only one API needed changing** — `sanitizeUser` is a **deny-list** so both profile paths already carried the field, while `StaffCatalogItemDTO` is an allow-list and didn't. **The finding that matters: staging holds `staffNumber` as the *number* `3`, and all three writers pad to a string** (schema `type: String`, `padStart(4,"0")`, the migration's `format()`), so **none of them wrote it** — the value's *shape* disproves its provenance. That narrows the 2026-08-01 contradiction on [[Sequential User Number - Atomic Allocation & Backfill]]: everything recorded there describes the **pre-rename `userNumber`**, the backfill has **not** run under the new name, and **where the `3` came from is unidentified**. Swept the correction through that note, [[TAT-450 TOR Certificate FE - Read Path Only]] (its "blank Authorization Number = unrun backfill" reading was overstated — the field isn't empty) and [[Index|work/Index]]. **Third piece, [[TAT-453 Assessor Eligibility - instructorType Load-Bearing Twice]]** — a **verification task, not a build one**: the backend narrowed the assessor pool and `AssessmentAssignForm` was already conformant, so the deliverable was establishing that no FE work was needed. **The finding is that `instructorType` — default `NONE`, backfill never run — is now load-bearing in a *second* subsystem** alongside the Form 32 A/B split, with neither ticket referencing the other. Three legacy conditions each empty the pool and a pre-existing `else` substitutes privileged roles, so it presents as **a working list of admins rather than a broken picker** — worse than [[TAT-429 Sit-In Eligibility & Move Semantics|TAT-429]]'s empty list, which at least got reported. New [[Patterns - Architecture & Boundaries|pattern]] on fallback branches masking empty filters. **Correction worth keeping: `instructorType` and `requestedRoleCodes` are two *different* unbackfilled fields** (32A/B vs 32C/D) — the dump conflated them, and they're entangled only by the ordering trap. Also: `4d993461` is labelled TAT-435 but contains TAT-453's code, so **git history can't be searched by ticket number here**. Split [[Index|work/Index]] at 25KB — the TAT-409 epic's 28 delivery entries moved verbatim to [[TAT-409 Delivery Log]]. **Fourth piece — a decision, [[TAT-450 AC-45 Superseded by TAT-455 - Certificate Not Auto-Released]]:** TAT-450 AC-45 requires QM approval to *"make the finalized certificate available to the instructor"*, TAT-455 AC-02/AC-03 require it to stay unpublished until an SA publishes — **two approved ACs, irreconcilable** (AC-45 would delete the publish gate entirely). **BA ruled TAT-455 correct, 2026-08-02**; no code change, the implementation already followed it. **AC-45 is still unedited in Jira, so QA testing TAT-450 literally will file a false defect.** This is the **second** instance of contradicting approved tickets after TAT-424 ↔ TAT-429 — but caught by *reading both specs against the code* rather than by a symptom months later, so it cost one BA question instead of an impossible onboarding path. New [[Patterns - Method & Conventions|pattern]]: an "Approved" stamp is a per-ticket act and never compares a ticket to the ones it overlaps. **Fifth piece — the second decision of the day: [[Form 32 C-D Fail-Open - Empty requestedRoleCodes on Legacy TORs|the fail-open stays open]]** until QA flags it, then fix or hand to Dawahreh. Deferring it surfaced the trap for whoever takes it: **`FORM_32_ROLE_BY_KEY` maps five keys, not four** — `TOR_CERTIFICATE` rides the same gate, so closing it would hide the Final TOR Certificate the week it was built. The branch is simultaneously narrower than it reads, because the next line (`if (!requiredRole) return true`) already allows every non-Form-32 template — its entire reach is those five keys. **Recommendation declined:** close it during the dev-DB clear, the only zero-cost window; the cost of that is that after the clear the symptom vanishes on its own and the guard *looks* correct, so nothing prompts a revisit until production data reintroduces the empty case — on a compliance surface whose failure direction is showing forms someone isn't authorised to hold.
- **2026-08-01** — **Backend drop consumed across two frontends; the Form 32 C/D fail-open root-caused.** Dawahreh shipped TAT-449/454/450 to [[tat-app-ws Backend]] with **no frontend at all**, and both integration guides described an FE state that did not exist — TAT-454's says to "keep sending `courseMethod`", which [[tat-ws]] had never sent, so the new eligibility filter was inert while assignment already hard-rejected on save. Built both halves: [[TAT-454 Instructor Assignment Filtering - courseMethod]] (`cf1a168`, **verified** — three `role=IN` calls carrying `courseMethod=Theoretical`, all 200, `role=EX` correctly omitting it) and [[TAT-450 TOR Certificate FE - Read Path Only]] (`9459415`, read path only — GET 200 and the page renders, but no signature/submit/QM save/approve has ever run). Then root-caused why instructors see Form 32C/32D: **not** missing role scoping — it exists and is enforced in four paths — but [[Form 32 C-D Fail-Open - Empty requestedRoleCodes on Legacy TORs|a gate that returns `true` for every form when `requestedRoleCodes` is empty]], with nothing backfilling legacy TORs. A newly created user filtered correctly, which is the discriminating contrast that pointed at data rather than logic. Corrected the 2026-07-05 gotcha that recorded the symptom as "license-scoped, not role-scoped" — body rewritten, not annotated. New [[Patterns - Architecture & Boundaries]]: a guard that fails open on absent data is disabled by the data, silently. Also ran [[Patterns]] and [[Q3 2026]] through a `/om-tidy` split (46KB and 42KB → domain notes + a July archive, 32 deep links retargeted) and fixed a hook bug where `work/meetings/README.md` counted as an unprocessed export, making that warning permanently unclearable.
- **2026-07-30** — **Loom shipped as a plugin; Form 32 A/B split by instructor type.** Two halves. **(1)** [[Loom]] left this vault for its own repo (`Black-Lotus98/loom`) as a Claude Code plugin + marketplace, so it can be handed to colleagues and scoped per project. Three real bugs fixed in it: `--merge` reported success while merging nothing (staged, never committed — the path had never been driven because dry-run is the default), a marketplace manifest that failed validation and would have broken every install, and worktrees with no dependencies. It then ran its **first live `codex` slice**, twice, for a byte-identical diff. **(2)** [[Instructor Type - Per-Authority Form 32 Split]] — TAT-451 + TAT-448. Every instructor saw both Form 32A and 32B because `FORM_32_ROLE_BY_KEY` mapped both to the single `IN` code. **I was wrong twice before I was right**, and both corrections came from reading code rather than reasoning harder: first "the backend has no role→form filtering" (it has had it all along, enforced in four paths), then "instructor type is per (authority × aircraft)" (it is per-TOR, and a TOR is already one-per-licence). Shipped the type, the gate, per-authority selection at create/edit, a backfill migration, an activation guard and an audit trail — and deleted a **fork of the rule** the FE had carried from a different input source. Three traps caught pre-ship: a nullable enum `tsc` accepted and `check:schemas` rejected (6th instance, first ever caught before production), a hidden form section whose defaults would have let an instructor wipe their own TOR types, and a guard that would never have fired because its query omitted the field. **Nothing is verified end to end** — the migration has never run and no audit row has ever been written.

- **2026-07-12** — **The sit-in deadlock, the assessor model, and three of my own bugs.** Long session across [[tat-app-ws Backend]] / [[tat-prereq]] / [[tat-ws]] (~38 commits).
  - **[[TAT-429 Sit-In Eligibility & Move Semantics]]** — a fresh instructor never appeared in Add Instructor. Root cause was a **circular dependency across four services**: TOR `ACTIVE` ← History Form approved ← sit-in final assessment ← a sit-in ← `addCourseInstructor` ← an **ACTIVE TOR**. Nobody could ever be onboarded, and the symptom was an *empty list*, which reads as "nobody qualifies" rather than "this gate cannot be passed". TAT-424's AC-09 had over-reached into TAT-429's bootstrap path; the two tickets contradict and each carries a lone rubber-stamp "Approved" 13 days apart. Fixed → **drove the cycle end to end for the first time in the product's history** → which surfaced 4 latent bugs behind the old deadlock.
  - **[[TAT-423 Assessment Report Rubric]]** — the form had **no assessor concept**; `assertCanEditReport` let the instructor *being assessed* grade and sign themselves. Added `assessorUserId` (optional — the same circular dependency, caught in design this time), an eligible-assessor endpoint, `/my-assessments`, the missing header row, and SA force-delete. Also **reversed my own auto-approve** after the BA rule "not approved until signed": SA/TM submitting had been flipping to `APPROVED` with an **empty `tmSection`** — an approved Part-147 assessment with no approver signature.
  - **[[History Form - Training & Validity Records]]** — opened up the hard-coded 5-course catalog: external training records + **online-course completions auto-added**. That feature turned out to be a `return` statement in an already-wired hook.
  - **[[Staff Management - Unreachable Backend Endpoints]]** — swept all 108 staff-management routes against every frontend. **Nobody can pause a TOR**; an instructor's **requested roles are frozen at creation**.
  - **Three mistakes of mine, all recorded**: I "re-confirmed" `tor.aircraftTypeIds` as a live keystone gap when **the vault already said it was fixed** (bad grep + trusting a stale Gotcha over newer notes); I broke the 35h badge by rewiring it to a collection storing 0 hours (**framed the question badly, got a reasonable answer to the wrong question, changed a compliance number without checking real data**); and my first nullable-enum CI check both false-positived *and* false-negatived on the very bug it was written for.
  - New [[Patterns]]: **the backend owns business rules, the FE renders them**; a wired hook with an empty branch is where a feature belongs. New [[Gotchas]]: soft-delete via `.save()` can't delete an invalid document; enum + `default: null` (5th instance → now CI-checked); a browser MIME type is not a file extension; `StaffSitIn.active` means "in progress", not "exists".

- **2026-07-08** — **Notification system, both repos.** Wired the full `/api/notifications/*` surface into [[tat-prereq]] (committed `d760e2b` on `dev`): navbar **bell + unread badge + real-time flashing dot** over **Socket.IO** (connect to the API *origin*, strip the `/api` prefix; auth via access token; room = user `_id`; event `"notification"`), plus a `/notifications/[id]` **detail page**. Backend ([[tat-app-ws Backend]] `07cd3fba` on `dev`): traced that `resolveClientBaseUrl` only knew dashboard + online-courses, so **staff-management notifications wrongly resolved to the admin dashboard**. Added a third client base URL (`STAFF_MANAGEMENT_URL`, env-driven → `staff.tat147.com`) keyed off `clientApp === 'staff-management'`, **repointed 21 prereq settings** to tat-prereq routes (tagged `client: staff-management`, baked at seed time), and **deep-linked Form 285/32 + assessments** by forwarding `torId` (responses already carried it via `toDTO`). Bootstrap only seeds *new* settings + re-seed is destructive, so wrote a **non-destructive migration** (`scripts/migrations/2026-07-08-...js`). New [[Patterns]]: notification links target the right frontend via an env-driven base URL. See [[TAT Notification System - Bell, Detail Page & Prereq Deep-Links]]. Reinforced the **no-comments** rule (shipped commented code, stripped it). Left Qusai's unrelated tat-prereq WIP (change-password, country select) unstaged.
- **2026-07-07** — **Form 285 (TAT-414) deep-dive + fixes.** Traced the whole Form 285 lifecycle FE→backend and found the [[tat-prereq]] backend is **fully implemented** (the reference note still calls it "net-new/unconfirmed" — needs updating). **Shipped FE** to [[tat-prereq]] `dev`: AC-08 (AM/SA edit-and-save before submit + editable Accountable Manager), signature rendered as an image (was a raw S3 key), role-gated Save/Submit actions, and **restyled the page to the official 180F-0285 document layout** (commit `be9067d`). **Shipped backend** to [[tat-app-ws Backend]] `dev` (commit `2cf90713`): the **accountable manager is now the approving user** (any user with the approve permission, e.g. SA), dropping the `ACCOUNTABLE_MANAGER`-role lookup — per Qusai's call that "whoever approves is the AM." **Bug found (not yet fixed):** Form 285 `GET` 404s because the read path over-constrains (`findOne` re-derives templateId + `instanceKey: "default"`) while duplicate 285 docs/templates exist for a TOR — same class as the keystone data issues; deferred a data backfill after it caused a **shared-dev-DB permission regression** (see new [[Gotchas]] — booting the app re-runs the **destructive** role-action seeder). New [[Patterns]]: **propose before implementing** (don't jump to code when Qusai asks "what/how/can we").
- **2026-07-06** — TAT-409 fix cycle: the backend shipped a fix drop (commit `db6922f7` "TAT Gap list" + TAT-422/423/424/429) — re-verified **18 of 37 gaps fixed**, incl. the **keystone** (`tor.aircraftTypeIds` now populated) and a new `requestedRoleCodes` concept using the **same role→form mapping** as [[tat-prereq]]'s FE gate. Then fixed + **shipped 6 FE gaps to `main`** (M5 cert preview, M6 request-course-online, M8 pagination, M9 server-side role gating, M12 AD editor, H8 2y window), **removed all dummy data + the dev-auth bypass**, and **verified live in-browser** (M5/M6/M9 confirmed). See [[TAT-409 Bug & Gap List]] resolution status. New [[Gotchas]]: sharing HTML in Teams (JS stripped + UTF-8 mojibake → prefer `.docx`); IDE TS-server falls back to `any` on React-Query data where batch `tsc` passes (fix with explicit param annotations).
- **2026-07-05** — Completed the full [[TAT-409 Ticket Groups & Inspection Map|TAT-409 functionality inspection]] (all 11 groups, backend + FE vs Jira ACs). Output: [[TAT-409 Bug & Gap List]] — 37 gaps by severity + platform + fix (`.md`/`.html`/`.docx`). Keystone defect: `tor.aircraftTypeIds` is never populated (cascades into 422 create, 424 eligibility pickers, Form 32 role-scoping). Also shipped the [[tat-prereq]] FE Form 32 role gate (hide A/B/C/D by the TOR owner's roles) + section-status recompute fix, verified in-browser. New [[Gotchas]]: Form 32 privileged-editor unimplemented (AD 403), History Form 3-approval eligibility chain + arbitrary sit-in evaluator, training-history-requires-approval divergence, static-HTML-for-Teams + docx-for-encoding.
