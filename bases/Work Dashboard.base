filters:
  and:
    - file.hasTag("work-note")
    - file.ext == "md"
    - '!file.inFolder("templates")'
formulas:
  quarter: if(date, "Q" + ((date(date).month - 1) / 3 + 1).floor().toString() + " " + date(date).year.toString(), "")
  age_days: if(date, (today() - date(date)).days, "")
  stale_days: (now() - file.mtime).days.round(0)
properties:
  file.name:
    displayName: Note
  status:
    displayName: Status
  project:
    displayName: Project
  formula.quarter:
    displayName: Quarter
  formula.stale_days:
    displayName: Days Untouched
  date:
    displayName: Date
  description:
    displayName: Summary
views:
  - type: table
    name: Active Work
    filters:
      and:
        - status == "active"
    groupBy:
      property: project
      direction: ASC
    order:
      - file.name
      - date
      - project
      - formula.quarter
      - description
  - type: table
    name: Backlog
    filters:
      and:
        - status == "backlog"
    groupBy:
      property: project
      direction: ASC
    order:
      - file.name
      - date
      - project
      - description
  - type: table
    name: All by Quarter
    groupBy:
      property: formula.quarter
      direction: DESC
    order:
      - file.name
      - date
      - status
      - project
      - description
  - type: table
    name: Stale Actives
    filters:
      and:
        - status == "active"
        - file.mtime < now() - "14 days"
    order:
      - file.name
      - formula.stale_days
      - date
      - project
      - description
    sort:
      - property: file.mtime
        direction: ASC
  - type: table
    name: Completed
    filters:
      and:
        - status == "completed"
    groupBy:
      property: formula.quarter
      direction: DESC
    order:
      - file.name
      - date
      - project
      - formula.quarter
      - description
